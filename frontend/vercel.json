{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "Backend API URL (Render backend URL + /api)",
      "value": "https://smart-foods-backend.onrender.com/api"
    }
  }
}
